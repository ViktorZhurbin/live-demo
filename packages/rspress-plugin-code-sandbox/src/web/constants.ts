export enum View {
	Preview = "Preview",
	Code = "Code",
	Split = "Split view",
}

export enum LocalStorage {
	Settings = "rspress-plugin-code-playground",
}

export enum SearchParams {
	SnippetId = "snippetId",
	ForkedFromId = "forkedFromId",
}
