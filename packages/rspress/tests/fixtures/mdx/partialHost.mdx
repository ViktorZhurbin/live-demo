import PartialDemo from "./_partialDemo.mdx";
import Missing from "./_notOnDisk.mdx";

# A page that imports a partial

The page itself has no demo. Everything the scan finds here it finds through
the import above.

<PartialDemo />
<Missing />
