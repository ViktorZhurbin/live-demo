# Non-Default External Import Demo

Its entry file imports `clsx`, a package that isn't in the plugin's
`defaultModules`, so it can only reach the virtual module through a real
scan.

<code src="../valid/WithExternalImports.tsx" />
