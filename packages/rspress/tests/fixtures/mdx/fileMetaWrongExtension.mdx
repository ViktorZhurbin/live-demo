# File Meta Wrong Extension

A `file=` reference with an extension outside the supported set. The target
doesn't need to exist on disk: the extension check rejects this before any
filesystem lookup.

```tsx file="../valid/SimpleComponent.py" live
```
