# Doc Root Prefix Demo Example

A `file=` reference using the `/` prefix, resolved relative to the doc root
(`config.root`) rather than this MDX file's own directory:

```tsx file="/valid/SimpleComponent.tsx" live
```
