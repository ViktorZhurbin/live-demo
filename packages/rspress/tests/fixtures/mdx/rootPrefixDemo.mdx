# Root Prefix Demo Example

A `file=` reference using the `<root>/` prefix, resolved relative to
`process.cwd()` rather than this MDX file's own directory:

```tsx file="<root>/tests/fixtures/valid/SimpleComponent.tsx" live
```
