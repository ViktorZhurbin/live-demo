# File Meta No Extension

A `file=` reference with no extension. Unlike the deprecated `<code src>`,
`file=` can't guess it — @rspress/core reads this path literally.

```tsx file="../valid/SimpleComponent" live
```
