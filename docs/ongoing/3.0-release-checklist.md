# 3.0 release checklist

## Docs

- [ ] **CHANGELOG.md**: `[Unreleased]` is already substantial and well-formed
      — retitle `[Unreleased]` → `[3.0.0] - <date>` once everything else here is
      done and no further changes are expected before publish.
- [ ] **README.md "Compared to `@rspress/plugin-playground`"**: has a
      "Coming in v3" bullet (per-page layout injection). This is v3 now —
      confirm it shipped (CHANGELOG's "Per-page layout injection" entry says
      yes) and fold it into the main improvements list instead of a
      forward-looking "coming" section.
- [ ] **README.md improvements-over-upstream claims**: CLAUDE.md already flags
      these need empirical verification before 3.0 ships. Check each bullet
      (multi-file support, TS w/o red squiggles, wrap/resize/view-switcher/
      fullscreen niceties) actually holds against the current upstream
      version, not the one this was originally compared to.
- [ ] **Version-tag guidance**: root CLAUDE.md already notes this —
      `packages/rspress/README.md` and `website/docs/guide/getStarted.mdx`
      currently say "v2 = current, install `@1` for Rspress v1". Once 3.0 is
      out, `@2` becomes the legacy tag and update both docs with the new row.

## Mechanics

- [ ] **`package.json` version**: currently `2.0.6` in
      `packages/rspress/package.json` — bump to `3.0.0` (this repo has no
      separate version-bump tooling/changeset visible, so this is likely a
      manual edit paired with the CHANGELOG retitle).
- [ ] **Full verification pass**: `pnpm check:all` (format + lint + build +
      typecheck + tests + knip + e2e) clean on the exact commit you intend to
      tag.
- [ ] **npm dist-tags**: `latest` should move to `3.0.0`
      immediately and the last `2.x` publish needs an explicit `@2`
      tag pointed at it.
- [ ] **Git tag**: existing tags are inconsistent (`v0.0.4`.. `v1.0.0` prefixed
      with `v`, but `2.0.4-rc.2`/`2.0.6` bare). We continue using bare for `3.0.0` onwards.
- [ ] **No `RELEASING.md` exists** in the repo. `pnpm run release` covers
      most of it, so a document was redundant.

## Not blocking, but worth a conscious yes/no

- [x] Anything in `.open-questions.md` that reads as "almost decided" — worth
      a final skim to confirm nothing there quietly needs to land before 3.0
      rather than after (none currently look release-blocking from a read of
      the file, but re-check once the above is done).
