---
title: Get started
---

## `rspress-plugin-live-demo`

This plugin turns MDX code blocks and external files into interactive playgrounds with code editor and live preview updates. Try editing code below:

<code src="./external/snippets/basics/Basic" />

## Installation
import { PackageManagerTabs } from 'rspress/theme';

<PackageManagerTabs command="install rspress-plugin-live-demo -D" />

## Usage

```ts title='rspress.config.ts'
import { defineConfig } from "rspress/config";
import { rspressPluginLiveDemo } from "rspress-plugin-live-demo";

export default defineConfig({
  plugins: [rspressPluginLiveDemo()]
})
```
