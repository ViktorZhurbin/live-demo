## Alignment with official plugins

- [ ] Would switching from the magic `<code src>` to explicit `<LiveDemo src>` reduce code complexity? Pros and Cons (one being UX)
  - `@rspress/plugin-playground` v2 moved from `<code src>` to `file="path/to/file"` code snippet meta, aligning with "inline" demos. See `.claude/.tmp/plugin-playground/docs-api.md`. Consider doing that too.

## Docs

- [ ] Replace the `goWild` example. It should look complex, not weigh a lot. Find something that reads as impressive and costs far less.
